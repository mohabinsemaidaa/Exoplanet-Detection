# Production requirements plus development-only tools:
# Jupyter (notebooks), pytest/httpx (tests), FastAPI/Uvicorn (API service),
# requests (dataset download script).
-r requirements.txt
requests==2.32.5
jupyter==1.1.1
fastapi==0.128.8
uvicorn==0.39.0
pytest==8.4.2
httpx==0.28.1
